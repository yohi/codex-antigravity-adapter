# Google OAuth2 Credentials for Antigravity
# Required for authentication with the Antigravity service

# Google OAuth Client ID
ANTIGRAVITY_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google OAuth Client Secret
ANTIGRAVITY_CLIENT_SECRET=your-client-secret

# State Secret for OAuth flow (HIGHLY RECOMMENDED)
# A persistent secret used to sign OAuth state parameters
# Without this, OAuth states will be invalidated on restart and multi-instance deployments will fail
# Generate a secure random string (e.g., openssl rand -hex 32)
ANTIGRAVITY_STATE_SECRET=your-secure-random-secret-here

# OAuth Scopes (Optional)
# Override the default scopes if needed (comma-separated)
# Default scopes are determined by IS_INTERNAL_ONLY flag in src/config/antigravity.ts
# Example: ANTIGRAVITY_SCOPES=https://www.googleapis.com/auth/userinfo.email,https://www.googleapis.com/auth/userinfo.profile
# ANTIGRAVITY_SCOPES=

# Project ID (Optional)
# If not set, will be auto-detected from API response
# ANTIGRAVITY_PROJECT_ID=your-project-id
